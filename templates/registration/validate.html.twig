{% extends 'base.html.twig' %}

{% block title %}Vérification requise — Blog Manager{% endblock %}

{% block body %}
<div class="min-h-[80vh] flex items-center justify-center px-4">
    <div class="max-w-md w-full text-center space-y-8 bg-white p-10 rounded-2xl shadow-xl border border-gray-100">
        
        {# Icône Illustration #}
        <div class="flex justify-center">
            <div class="bg-indigo-100 p-4 rounded-full">
                <svg class="h-12 w-12 text-indigo-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
            </div>
        </div>

        {# Textes #}
        <div>
            <h1 class="text-3xl font-extrabold text-gray-900 tracking-tight">Vérifiez vos emails</h1>
            <p class="mt-4 text-gray-600 leading-relaxed">
                Un lien de confirmation vient de vous être envoyé. Veuillez cliquer dessus pour activer pleinement votre compte Blog Manager.
            </p>
        </div>

        {# Affichage des erreurs Flash (Si le lien a expiré par exemple) #}
        {% for flash_error in app.flashes('verify_email_error') %}
            <div class="bg-red-50 border-l-4 border-red-500 p-4 text-left">
                <p class="text-sm text-red-700 font-medium">{{ flash_error }}</p>
            </div>
        {% endfor %}

        <div class="pt-6 border-t border-gray-100 space-y-4">
            <p class="text-sm text-gray-500">
                Vous n'avez rien reçu ? Vérifiez vos courriers indésirables (spams).
            </p>
            
            <div class="flex flex-col sm:flex-row gap-3 justify-center">
                <a href="/" class="text-sm font-semibold text-gray-900 hover:text-indigo-600 transition">
                    Retour à l'accueil
                </a>
                <span class="hidden sm:inline text-gray-300">|</span>
                <a href="mailto:hounkpatindewanou@gmail.com" class="text-sm font-semibold text-gray-900 hover:text-indigo-600 transition">
                    Contacter le support
                </a>
            </div>
        </div>

    </div>
</div>
{% endblock %}